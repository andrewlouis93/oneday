<% if @todo.valid? %>
	$('<%= j(render(@todo)) %>').hide().prependTo(".todos").fadeIn(500);
	
 	/*$('.todos').animate({scrollTop: $('.todos')[0].scrollHeight},700); */
	
	console.log($(".todos li:last-child").filter(":visible").position().top);

	$(".ajax_submit").val('')
<% else %> 
	$(".ajax_submit").val('')
	alert('Something went wrong!');
<% end %>